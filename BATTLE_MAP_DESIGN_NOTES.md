# 战斗地图系统设计笔记

这份文档用于暂存目前关于战斗地图系统的想法。现阶段先记录方向，不急着实现完整系统，避免过早把战斗、地图、装备标签和事件生成绑得太死。

## 目标

战斗地图不只是背景，而是可以参与玩法的战场：

- 提供边界、障碍、刷怪点、玩家出生点等基础战斗信息。
- 支持随机生成少量可互动地图物件，例如苹果树、木箱、矿石、祭坛。
- 支持特殊地形，例如岩浆、冰面、毒沼、治疗草地。
- 未来让玩家构筑中的装备 tag 影响战斗地图生成，让构筑从数值影响扩展到环境影响。

## 双网格系统的定位

双网格系统适合用于地图视觉过渡，但不建议一开始把全部战斗逻辑都建立在双网格上。

适合双网格处理的内容：

- 草地、泥地、石地、水面等地形交界。
- 岩浆、冰面、毒沼等特殊地形边缘。
- 程序化地图生成后的自动修边。
- 让随机地图看起来不那么像方块拼接。

不建议双网格直接负责的内容：

- 玩家和怪物是否能走。
- 技能是否命中。
- 敌人寻路。
- 战斗胜利条件。
- 交互物是否被攻击、掉落物是否生成。

推荐做法是“逻辑地图”和“视觉地图”分离：

- 逻辑层记录格子类型、可走性、危险区域、刷怪区域。
- 视觉层用 TileMap 或双网格根据逻辑层生成更自然的地表表现。

## 地图交互物件

苹果树、木箱、矿石、爆炸桶这类对象不建议做成 TileMap 瓦片本身，而建议做成独立场景。

推荐概念：

- `BattleMapObject`
- `DamageableMapObject`
- `HarvestableObject`

它们可以是轻量可受击实体，拥有：

- 生命值。
- 受击反馈。
- 死亡或破坏逻辑。
- 掉落物逻辑。
- 碰撞区域。

如果当前伤害系统只识别 `Entity`，短期可以让这类对象继承轻量版 `Entity`，但不要加入 `player` 或 `enemy` 阵营，也不要参与索敌和胜利条件。

苹果树示例流程：

1. 战斗地图生成时，在合法地块上随机生成苹果树。
2. 玩家攻击苹果树。
3. 苹果树受伤或被破坏时，有概率生成苹果掉落物。
4. 玩家拾取苹果后恢复少量生命。

## TileMap 适合负责的内容

TileMap 更适合作为地图底层结构：

- 地面类型。
- 地图边界。
- 不可通行障碍。
- 危险地形区域。
- 地图装饰底图。
- 刷怪点和出生点标记。

独立场景更适合负责：

- 可攻击物件。
- 可拾取物。
- 陷阱。
- 祭坛。
- 爆炸桶。
- 临时生成的技能地面效果。

## Tag 影响地图生成

玩家装备 tag 影响战斗地图生成是可行的，而且很适合本项目的肉鸽构筑方向。

示例：

- `火炎` tag 多：地图额外生成岩浆地块、火焰桶、燃烧裂隙。
- `寒霜` tag 多：地图额外生成冰面、冰晶、寒雾区域。
- `疗愈` tag 多：地图额外生成苹果树、草药、生命泉。
- `原料` tag 多：地图额外生成采集物或矿石。
- `诅咒` tag 多：地图生成更多危险地形，但可能伴随更高奖励。
- `洞察` tag 多：显示隐藏资源点，或提高特殊互动物出现概率。

建议不要在地图生成器中直接写大量 `if tag == xxx`。更好的方式是用资源驱动规则。

未来可以设计：

- `BattleMapGenerationContext`：记录本场战斗、玩家 tag 计数、房间等级、地图主题等信息。
- `BattleMapRule`：一条地图生成规则资源。
- `BattleMapObjectSpawnRule`：生成苹果树、木箱、矿石等物件。
- `BattleMapTerrainRule`：生成岩浆、冰面、毒沼等地形。

规则资源可包含：

- 需要的 tag。
- 每几点 tag 增加一次生成尝试。
- 生成对象或地形。
- 最大生成数量。
- 最小安全距离。
- 是否阻挡移动。
- 权重。

## 分阶段计划

第一阶段：基础互动物件

- 完成苹果树。
- 完成苹果掉落物。
- 玩家拾取苹果恢复生命。
- 地图上随机摆放少量苹果树。

第二阶段：地图基础数据

- 创建 `BattleMapData` 资源。
- 记录战斗地图场景、出生点、刷怪点、边界、可生成区域。
- 让 PlayScene 或 EnemySpawner 能读取地图数据。

第三阶段：特殊地形

- 添加岩浆地块。
- 添加冰面或减速地块。
- 添加毒沼或持续伤害区域。
- 地形效果复用现有伤害、status、modifier 系统。

第四阶段：tag 驱动生成

- 先选择 2 到 3 个 tag 试做，例如 `火炎`、`疗愈`、`诅咒`。
- 用规则资源生成额外地形或互动物。
- 验证玩法是否有趣，再扩展更多 tag。

第五阶段：视觉优化

- 在逻辑地图稳定后，再考虑引入双网格视觉过渡。
- 优先用于地形边缘修饰，不直接承担战斗逻辑。

## 当前建议

当前项目内容已经比较丰富，地图系统不要一开始做得太大。更稳的路线是先做一个小而完整的互动闭环：

苹果树 -> 被攻击 -> 掉苹果 -> 玩家拾取 -> 回血。

这个闭环能验证：

- 地图物件能否被技能命中。
- 地图物件是否应该继承 `Entity`。
- 掉落物和拾取逻辑是否顺手。
- 战斗地图随机摆放对象是否影响战斗节奏。

等这个闭环好玩，再把 tag 影响地图生成接进去。
