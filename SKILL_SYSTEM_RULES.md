# 技能系统规则

## 1. 技能阶段

- 技能不再通过 `Lv.1`、`Lv.2` 数字递增来升级。
- 每个 `SkillData` 可以在 `upgrade_options` 中配置一个或多个升级目标。
- 选择升级奖励后，原有的 `SkillEntry.skill_data` 会被替换为升级目标资源。
- `upgrade_options` 为空时，技能处于最终阶段。
- 因此，一个基础技能可以拥有多个并列升级分支，例如：
  - `强健肉体 -> 钢铁之躯`
  - `强健肉体 -> 完美体魄`
- `SkillEntry.level` 暂时保留，用于兼容旧存档，但不再驱动新技能升级逻辑。

## 2. 技能容量

- 基础攻击不计入主动技能拥有上限，也不能被卸下。
- 玩家最多拥有 6 个永久主动技能。
- 玩家战斗中最多携带 4 个永久主动技能，基础攻击始终额外占据第一个运行时技能槽。
- 装备、状态或事件提供的临时主动技能不计入 6 个拥有上限，也不计入 4 个携带上限。
- 被动技能最多拥有 2 个。
- 当被动技能栏已满时，获得新被动技能会替换最早获得的被动技能；奖励描述会提示替换对象。

## 3. 升级奖励阶段

普通战斗胜利后的奖励阶段按以下顺序循环：

1. 被动技能阶段
2. 主动技能阶段
3. 主要属性提升阶段

其他规则：

- 每次提供 5 个奖励选项。
- 当前拥有技能存在升级分支时，对应的升级奖励优先出现在最前面。
- 剩余位置从当前阶段的技能池中随机抽取。
- 当前阶段没有足够的合法技能时，用角色主要属性奖励补足。
- 普通被动/主动阶段最后一个选项固定为属性提升，作为不升级或不替换技能时的保底选择；技能类别已满时同样满足该规则。
- 升级奖励刷新只重新抽取当前阶段，不会改变阶段顺序。
- 初始技能选择是独立流程，只从主动技能池中抽取，不推进普通奖励阶段。

## 4. 主动技能携带

- 背包打开时，技能面板显示当前携带技能和全部永久主动技能。
- 点击主动技能列表中的条目可以切换“携带中/未携带”。
- 携带栏满时，新的技能不会自动挤掉旧技能，玩家需要先卸下一个技能。
- 基础攻击和临时主动技能显示在列表中，但不能被卸下或占用普通携带名额。

## 5. 存档兼容

- 新增的主动技能拥有上限、携带上限、被动技能上限和奖励阶段都会写入存档。
- 旧存档缺少这些字段时，读取时使用默认值：主动拥有 6、主动携带 4、被动 2、奖励阶段为被动阶段。
- 已保存的升级奖励会保留其目标技能和替换来源，继续游戏时不会重新随机。

## 6. 后续技能树界面

技能树查看界面暂不在本次改动中实现。后续可以直接读取每个技能的 `upgrade_options`，按来源技能与目标技能绘制连接线，并支持主动/被动、基础/升级阶段筛选。
